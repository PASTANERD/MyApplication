<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.a0xbistrot.myapplication">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="한글도 되나?"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">
        <activity android:name=".MainActivity" />
        <activity android:name=".AnotherActivity" />
        <activity android:name=".MemoActivity" />
        <activity android:name=".Restaurant" />
        <activity android:name=".MenuActivity" />
        <activity android:name=".ParkingLot" />
        <activity android:name=".AdvancedParkingLot" />
        <activity android:name=".DialogActivity" />
        <activity android:name=".CustomViewActivity" />
        <activity android:name=".NewWord" />
        <activity android:name=".AddingWordActivity" />
        <activity android:name=".WebActivity">

        </activity>
        <activity android:name=".WebMenuActivity" />
        <activity android:name=".CanvasActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>